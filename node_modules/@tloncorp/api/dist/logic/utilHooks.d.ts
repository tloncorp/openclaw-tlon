/**
 * Creates a ref whose current value is always the value as of last render.
 * Useful for preventing callbacks from changing and triggering rerenders.
 */
export declare const useLiveRef: <T>(value: T) => any;
/**
 * Minimizes churn for queries that return similar results repeatedly. Uses
 * react-query's `replaceEqualDeep` to ensure that we only return new objects
 * when the data belonging to those objects has changed.
 */
export declare function useOptimizedQueryResults<T extends {
    id: string;
}>(value: T[] | null | undefined): any;
export declare function useDebouncedValue<T>(value: T, delay: number): any;
export declare function useMutableRef<T>(initialValue: T): {
    current: T;
};
export declare function useMutableCallback<Args extends unknown[], Result>(callback: (...args: Args) => Result): (...args: Args) => Result;
//# sourceMappingURL=utilHooks.d.ts.map