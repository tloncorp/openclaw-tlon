import * as db from '../db';
export type Id = 'debugJson' | 'quote' | 'startThread' | 'muteThread' | 'viewReactions' | 'copyRef' | 'copyText' | 'edit' | 'report' | 'visibility' | 'delete' | 'forward' | 'summarize';
/**
 * Info about a channel action type that should not change based on context.
 */
export interface StaticSpec {
    actionType?: 'destructive';
    isNetworkDependent: boolean;
}
export declare function channelActionIdsFor({ channel, canWrite, }: {
    channel: db.Channel;
    canWrite?: boolean;
}): Id[];
export declare function staticSpecForId(id: Id): StaticSpec;
//# sourceMappingURL=ChannelActions.d.ts.map