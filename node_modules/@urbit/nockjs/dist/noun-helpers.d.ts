import { enjs } from "./noun-enjs";
import { dejs } from "./noun-dejs";
import { Atom, Noun } from "./noun";
declare const dwim: typeof import("./noun-dejs").dwim;
export type FaceMask = string | FaceMask[];
export type FaceAxes = {
    [key: string]: Atom;
};
declare const experimental: {
    mask: (face: FaceMask, axis?: Atom, axes?: FaceAxes) => FaceAxes;
    grab: (axes: FaceAxes, noun: Noun, face: string) => Noun;
};
export { enjs, dejs, dwim, experimental };
